#spring Configuration 目前只有一个配置文件,如果有需要,可以分为dev和prod,然后再通过spring.profiles.active=prod 来切换生产或开发环境的配置文件.
spring:
    profiles:
        active: default #控制不同环境的不同配置文件

    datasource:
        name: test
        url: jdbc:mysql://eslog.syamwu:3306/xcs_db?useUnicode=true&characterEncoding=UTF-8&useSSL=false #注意!
        username: xcs
        password: xcs2018
        # 使用druid数据源
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.jdbc.Driver
        filters: stat
        maxActive: 20
        initialSize: 1
        maxWait: 60000
        minIdle: 1
        timeBetweenEvictionRunsMillis: 60000
        minEvictableIdleTimeMillis: 300000
        validationQuery: select 'x'
        testWhileIdle: true
        testOnBorrow: false
        testOnReturn: false
        poolPreparedStatements: true
        maxOpenPreparedStatements: 20

#Server Configuration
server:
  port: 8077
  context-path: /


#Logback Configuration
logging:
   config: classpath:logback.xml

# Mybatis Configuration
mybatis:
   mapper-locations: classpath:mybatis/mapper/*.xml
   
storey: 
   key: query.bool.must.match_phrase.appname.query
